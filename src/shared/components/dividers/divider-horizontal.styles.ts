import { StyleSheet } from 'react-native';
import { MAIN_THEME } from '../../constants';

export const styles = StyleSheet.create({
  divider: {
    width: '100%',
    height: 1,
    backgroundColor: MAIN_THEME.dividerBackgroundPrimary,
  },
});
